# my-snake-game

簡單的網頁版貪吃蛇遊戲。

## 如何執行

在專案目錄啟動本地伺服器：

```bash
python3 -m http.server 8000
```

然後打開瀏覽器：

- `http://127.0.0.1:8000`

> 直接雙擊 `index.html` 也可開啟，但建議使用本地伺服器避免部份環境載入限制。

## 操作方式

1. 點擊 **Start** 開始遊戲。
2. 使用 **方向鍵** 或 **WASD** 控制蛇。
3. 點擊 **Reset** 可重置。
